# Selenium and Java Petstore project
created on the basis of the javastart course

## Requirements
JDK  
maven  
docker  
docker-compose


## Configuration
`docker-compose up -d` in the main directory  
`docker-compose down` after test

## Run test
`mvn test`
